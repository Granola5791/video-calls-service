server:
  listen_addr: "localhost:8082"
  frontend_addr: "http://localhost:5173"
  api:
    stream_from_client_path: "stream/:meeting_id"
    stream_to_client_path: "get-stream"
    create_meeting_path: "create-meeting"
    join_meeting_path: "join-meeting/:meeting_id"

stream:
  read_buffer_size: 4096
  write_buffer_size: 4096
  got_chunk_msg: "Received binary data of length: %d\n"
  ready_msg: "READY"

errors:
  forbidden: "We don't allow your kind in here."
  internal: "Something went wrong on our side."

meeting:
  meeting_id_name: "meeting_id"
  dir_path: "./meetings"
  dir_perms: 0755
  token_name: "meeting_token"

auth_jwt:
  token_cookie_name: "auth_token"
  user_id_name: "user_id"

logger:
  ffmpeg_log_file: "./ffmpeg.log"

keep_alive:
  exp_name: "exp"
  token_cookie_name: "keep_alive_token"